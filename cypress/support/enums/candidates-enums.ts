export enum RECRUITMENT_TABS {
    RECRUITMENT = "Recruitment",
    CANDIDATES = "Candidates",
    VACANCIES = "Vacancies",
}

export enum RECRUITMENT_BUTTONS {
    ADD = "Add",
    SHORTLIST = "Shortlist",
    SCHEDULE_INTERVIEW = "Schedule Interview",
    MARK_PASSED = "Mark as Passed",
    SAVE = "Save",
}

export enum PLACEHOLDERS {
    TYPE_HINTS = "Type for hints...",
    DATE = "yyyy-mm-dd",
    TIME = "hh:mm",
}

export enum INPUT_NAMES {
    FIRST_NAME = "firstName",
    MIDDLE_NAME = "middleName",
    LAST_NAME = "lastName",
    EMAIL = "email",
    CONTACT_NUMBER = "contactNumber",
    KEYWORDS = "keywords",
}

export enum TOAST_MESSAGES {
    SAVED = "Successfully Saved",
    UPDATED = "Successfully Updated",
    DELETED = "Successfully Deleted",
}

export enum COMMENTS {
    STRONG_CANDIDATE = "Strong candidate",
}
export enum JOB_LABELS {
    JOB = "Job",
    SUCCESS = "Successfully",
}

export enum BUTTONS {
    SAVE = "Save",
    SEARCH = "Search",
}

